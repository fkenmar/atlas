---
description: Create the next-numbered ADR
argument-hint: <decision title>
---
Create the next-numbered ADR in docs/adr/ titled "$ARGUMENTS", using the **adr-writing** skill (Context / Decision / Consequences, one page maximum, append-only).

1. Find the next number: highest existing `docs/adr/NNNN-*.md` plus one, 4 digits zero-padded.
2. If the Context isn't already clear from this conversation, interview me briefly first — 2 or 3 pointed questions maximum (what forced the decision, what alternatives were weighed, why did they lose). The Context section is the part future sessions need most.
3. Write the ADR, then confirm the file path. Do not edit any existing ADR — if this decision supersedes one, say so inside the new ADR's Decision section.
