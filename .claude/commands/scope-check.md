---
description: Check my last request against the non-goals contract
---
Evaluate my most recent request against the scope contract in CLAUDE.md (the PRD §3.2 non-goals: semantic search/embeddings, LSP-style code intelligence, IDE plugins/GUI, code editing/generation, languages beyond the Tier 1/2 list).

If it violates the contract:
1. Say so plainly — name the specific non-goal it hits, no hedging.
2. Append it to ideas.md under "Parked" with a one-line rationale and today's date.
3. Suggest the closest in-scope alternative, if one exists.

If it doesn't violate the contract, say so — and note which milestone it belongs to, since in-scope can still be NOT-YET (check the STATUS.md board).
